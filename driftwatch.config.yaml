# driftwatch configuration
# Converted from a module config by `driftwatch --migrate-config`.
# The original is still there; delete it once this file looks right.

sources:
  - 'docs/agents/**/*.md'
  # The glossary is a document about this repository, and every path it names is
  # meant to be here. That is what `path/missing` is for.
  - 'CONTEXT.md'
