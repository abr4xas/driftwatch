/**
 * Registro estatico de checks. ARCHITECTURE.md § Extensibilidad: sin carga
 * dinamica de plugins en v1, asi que agregar un check es agregar una entrada
 * aca y un archivo al lado.
 */
export const CHECK_IDS: readonly string[] = []
